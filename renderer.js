const nextButton = document.getElementById('next-button');

nextButton.addEventListener('click', () => {
  // Kode ini akan mengarahkan window aplikasi ke halaman template.html
  window.location.href = 'template.html';
});